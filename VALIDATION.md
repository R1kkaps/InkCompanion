# 1.4 大字天气边栏验收状态 · 2026-09-15

- 边栏厚度范围收紧为 34–48 px，新安装及旧版设置迁移后默认 48 px；日期、星期、温度、天气文字和电压字号均放大。
- 晴天按 Open-Meteo `is_day` 显示太阳或月牙；少云同样区分太阳云和月亮云，云、雨、雪、雷、雾使用独立粗线/实心图标。
- 图标轮廓参考 Lucide Sun、Moon、Cloud Sun、Cloud Moon 的统一 24 px 结构，针对 1-bit 输出重新实现，没有打包外部图标字体或运行时依赖。
- 竖栏底部及横栏末端新增有序点阵渐隐。密度从约 37.5% 逐步降至 0%，最终图像仍只含纯黑、纯白（或主画面的红色）。
- 49 项自动测试通过，新增昼夜图标差异、48 px 上限和渐隐密度检查；真实 Qt 控件测试覆盖默认 48 px。
- 已目视检查 48 px 左右竖栏、上下横栏、白天太阳、夜间月亮和全部九种天气图标，无文字或图标裁切。

# 1.3 倒装与流畅度修正验收状态 · 2026-09-14

- UI 默认方向改为 180°，启动预览直接反向；主图与边栏整帧旋转，边栏位置按实体安装后的方向计算。旧设置进行一次 180° 迁移，迁移后仍保存用户手动选择。
- 预览改为单后台 worker 和 90ms 合并刷新；worker 忙碌时不累积旧画面，只在完成后计算最新状态。保存和发送会等待最新预览，避免发出旧像素。
- 亮度/对比度处理改为与原公式等价的 256 项查表。当前机器基准：黑白 Floyd 约 579→480ms，三色 Floyd 约 713→598ms，Bayer 约 363→203ms，无抖动约 170→54ms。
- 预览窗在 150% Windows 缩放和 1048×740 可用窗口下完整显示；说明文字与图像不重叠。
- 47 项自动测试、真实 Qt 控件测试和旧设置迁移复测通过；后台重算期间 10ms UI 心跳最大间隔约 35ms。

# 1.2 信息边栏验收状态 · 2026-09-14

- 新增可选上、下、左、右信息边栏；默认 34 px，可调到 80 px。边栏为主图保留空间，四种位置的最终输出均保持 400×300。
- 边栏显示日期/星期、天气图标与温度、设备实测电压。竖向 34 px 使用分段布局，横向 34 px 使用单行布局。
- 天气通过 Open-Meteo 地理编码和当前天气接口读取，每 30 分钟更新；成功结果缓存到 `%LOCALAPPDATA%\InkCompanion\weather.json`，请求失败时保留旧结果。
- 45 项自动测试通过，覆盖四个边栏位置、严格黑白量化、天气代码映射、城市查询流程、三色编码和 BLE 连接路径；真实 Qt 控件测试通过。
- 四个方向的 400×300 输出已逐张目视检查，无裁切、覆盖或灰色像素。预览保存在 `research/sidebar-right.png`、`sidebar-left.png`、`sidebar-top.png`、`sidebar-bottom.png`。
- 使用真实 Open-Meteo 请求验证“上海”查询成功，返回当前温度、天气代码和 `Asia/Shanghai` 时区。

# 1.1 三色修正验收状态 · 2026-09-14

- 已从官网当前前端实现核对 BWR 编码和预览解码：红平面低有效，`0=红色`、`1=非红色`；全 `0xFF` 清除旧红色。此前 1.1 候选实现的极性说明及发送值错误，现已修正。
- 三色抖动改为官网的黑/白/红 RGB 最近色量化和逐 RGB 通道误差扩散；支持 Floyd–Steinberg、Jarvis、Stucki、Burkes、Sierra、Atkinson、Bayer 及强度调整。
- 自由缩放/平移裁切：缩放 0.1–5 倍、水平/垂直位置 ±1 帧；预览滚轮缩放、左键拖拽定位、双击重置。
- 41 项自动测试通过，包括网页 Floyd–Steinberg 固定像素样例、全部七种算法只输出面板色、低有效红平面、关闭红色时发送全 `0xFF`，以及等价画面的槽位缓存；真实 Qt 控件测试通过。
- 用户确认修正后的三色测试图显示完整，方向及黑白红逻辑正确。
- 21:31:45 实机连接成功，读取到槽位掩码 `0xF`；21:31:47 明确复用此前由墨伴创建的槽位 1，没有占用新槽位或覆盖原始槽位 0。
- 21:31:55 黑白红双平面传输与刷新命令完成，设备未返回错误；证据为 `research/bwr-screen-test-20260914T133041Z.json/.log`。

# 当前验收状态 · 2026-09-14

- 1.0 验收候选版：32 项自动测试通过；实际 Qt 控件测试通过；打包 EXE 冒烟启动正常、退出码 0。
- 已实际完成 GATT 连接、枚举与固件读取；已初始化读取驱动 0x16、MTU 分包 244、RLE、64 个 Flash 槽位，原槽位 0 已占用。
- 加入空槽位选择与本次会话图像缓存；不覆盖连接前占用的槽位。跨会话槽位回收尚未实现。
- 20:20:24 第三次连接成功；SID 兼容性校验通过，驱动保持 0x16。
- 20:20:25 选择空槽位 1，原槽位 0 保留；黑白图及白色红平面传输完成，20:20:27 提交刷新。
- 固件通知 usedMask 从 0x1 变为 0x3、currentSlot=1；无设备错误回复。
- 已等待 15 秒后断开；用户随后确认测试图“显示完整，方向和黑白正确”，1.0 黑白路径实机验收通过。
- 实机证据：research/screen-test-20260914T121550Z.json / .log / .connect.log。
- 网易云实际封面仍待播放器提供 SMTC 会话后的验证。
- 下方是按时间保留的历史记录；其中旧版本的只读限制或未测试说明只适用于当时版本。

# 验证记录 · 2026-09-14

## 后续 BLE 实机验证（13:19 香港时间）

- 按用户要求改为 UUID callback + asyncio.Queue + 新鲜 BLEDevice + 实際地址类型。
- 13:19:09 开始监听，13:19:46 捕获目标：名称为空，无 Scan Response，Random，RSSI -57。
- 捕获后约 1.8 ms 开始连接，第 1 次尝试成功，约 13.15 秒后完成 GATT 和版本读取。
- 读取固件 `v1.10-om6626`，枚举 2 个服务、5 个 characteristics。
- 全程无 INIT、无 CCCD 写入、无图片写入、无官网请求、无驱动/注册表/配对修改。
- 原始记录：`research/ble/gatt_20260914T051909_265994Z.jsonl`。
- GATT 清单：`research/ble/gatt_20260914T051909_265994Z.gatt.json`。
- 新增测试覆盖无名广播、Random/Public、换地址重试、队列合并、取消清理、错误代码、只读门控。
- 随后真实 Qt 桌面后台 worker 再次连接成功，第 1 次尝试即读到相同固件。
  界面与读取结果：`research/desktop-gatt.png`、`research/desktop-gatt.json`。
- 最终 24 项自动测试通过；新版 `InkCompanion-BLE-ReadOnly.exe` 启动冒烟测试退出码 0。

以下为初版交付时的历史记录；其中“尚未读取固件”已被本次结果更新，屏幕写入仍未验证。

## 已完成

- 16 项核心测试通过：像素位序、逐行填充、黑白极性、反转、旋转、镜像、透明背景、
  音乐排版、旧/新分包标志、压缩往返、多种分包大小、通知解析、断包失败、取消、校验门控。
- 真实 Qt 控件测试通过：反转和旋转改变实际像素；幻灯片切图、停止；歌曲封面排版；正常退出。
- 源码版与 PyInstaller 免安装 EXE 均已实际启动、截取自身窗口、正常退出。
- Windows 蓝牙扫描能正常发现附近广播；目标设备未出现。
- 普通 Windows 用户环境能读取系统媒体会话及实际缩略图字节。
  当时会话来源是 Edge，不能据此声称网易云已验证。沙箱中接口不可用，属于环境隔离。

## 尚未完成

用户确认墨水屏尚未打开，并要求先交付、暂停实机测试。
因此尚未读取 NRF_EPD_71E9 的真实驱动/固件、执行其校验、传输图像、观察方向/残影/刷新时间，
也未验证网易云播放器的封面会话。当前 400×300 配置来自官网 4.2 寸预设。

启动程序不会自动连接设备、监听音乐或自动发送。请在准备好设备后手动开启相应功能。
本机发布包在 `dist/InkCompanion`；整个文件夹需一起保留。

## 复现

```powershell
.\.venv\Scripts\python.exe -m unittest -v test_core
.\.venv\Scripts\python.exe ui_verify.py
.\dist\InkCompanion\InkCompanion.exe --smoke --screenshot research\exe-preview.png
```

UI 测试图：`research/app-preview.png`、`research/music-preview.png`、`research/slides-preview.png`。
示例封面是程序生成的测试图，不是真实歌曲封面。
# 1.5 Apple 风格界面验收状态 · 2026-09-15

- 依据 emilkowalski/skills 中 `emil-design-eng` 与 `apple-design` 的界面原则，重做窗口层级、分组表面、分段标签、按钮状态和输入控件。
- 使用 Windows 原生 Segoe UI Variable / Microsoft YaHei UI 字体栈，保持与系统一致的中文渲染；主操作使用 Apple 蓝，状态使用语义色。
- 取消按钮按下时的内边距位移和多层预览阴影，点击反馈即时但不引发布局跳动；频繁的预览拖动和缩放没有新增动画。
- 运行时生成的小图标保存到 `%LOCALAPPDATA%\InkCompanion\ui-assets`，应用位于只读目录时仍可启动。
- 49 项自动测试、真实 Qt 控件验证和源码版截图冒烟检查通过；BLE、图像处理、三色、旋转与边栏业务逻辑未改动。
